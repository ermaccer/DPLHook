#pragma once

class CGameOverlayManager {
public:
	void Draw(int Viewport);

	void DrawHooked(int Viewport);
};


class COverheadMap {
public:
	void Draw(int Viewport);

	void DrawHooked(int Viewport);
};